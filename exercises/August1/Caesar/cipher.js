var readline = require("readline-sync");
var input = readline.question('What phrase would you like to encrypt? ').toLowerCase();
var shift = parseInt(readline.question("How many letters would you like to shift? "));

//create a placeholder string
//var results = "";
//loop through the string
//var alphabet = "abcdefghijklmnopqrstuvwxyz";
//for each character
	//find the index of the current letter in the alphabet Use .indexOf
	//add shift to index of current letter
	//add new letter (alphabet[shift + oldIndex]) to our results string.
		//if new value (shift + currentIndex) > 25
			// subtract 26 from the new index
	//add new letter (alphabet[newIndex]) to our results string
//console.log (results string);



//continue in a for loop start over 